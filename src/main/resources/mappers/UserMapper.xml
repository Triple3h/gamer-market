<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qf.gamer.mapper.UserMapper">
  
  <resultMap id="UserResultMap" type="com.qf.gamer.domain.entity.User">
    <!--@mbg.generated-->
    <id column="uid" jdbcType="INTEGER" property="uid" />
    <result column="user_name" jdbcType="VARCHAR" property="userName" />
    <result column="password" jdbcType="VARCHAR" property="password" />
    <result column="join_date" jdbcType="TIMESTAMP" property="joinDate" />
    <result column="last_login" jdbcType="TIMESTAMP" property="lastLogin" />
    <result column="is_super" jdbcType="INTEGER" property="isSuper" />
    <result column="is_delete" jdbcType="INTEGER" property="isDelete" />
  </resultMap>

     

  
  <!--  登录查询 -->
  <select id="findByName" resultMap="UserResultMap">
    <!--@mbg.generated-->
    select
    user_name, `password`,is_super
    from user
    where user_name = #{usreName}
  </select>

  <!-- 注册 注意不能重复 -->
  <insert id="save">
    insert into user(user_name,password) values(#{userName},#{psd})
  </insert>


</mapper>